export interface DrawerProps {
    isOpen: boolean;
    handleToggle: any;
}

export interface MenuLinkProps {
    linkTo: string;
    linkTxt: string;
}

export interface MenuProps {
    isMobile: boolean;
}